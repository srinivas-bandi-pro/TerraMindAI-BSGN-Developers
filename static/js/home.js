(function(){function count(element){const target=Number(element.dataset.target),decimals=(element.dataset.target.split('.')[1]||'').length,start=performance.now(),duration=1300;function tick(time){const progress=Math.min((time-start)/duration,1),value=1-Math.pow(1-progress,3);element.textContent=(target*value).toFixed(decimals);if(progress<1)requestAnimationFrame(tick)}requestAnimationFrame(tick)}document.addEventListener('DOMContentLoaded',function(){const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,items=document.querySelectorAll('[data-reveal],[data-counter]');if(reduced||!('IntersectionObserver'in window)){document.querySelectorAll('[data-reveal]').forEach(el=>el.classList.add('is-visible'));document.querySelectorAll('[data-counter]').forEach(count);return}const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;if(entry.target.hasAttribute('data-reveal'))entry.target.classList.add('is-visible');if(entry.target.hasAttribute('data-counter'))count(entry.target);observer.unobserve(entry.target)}),{threshold:.12});items.forEach(item=>observer.observe(item))})}());
