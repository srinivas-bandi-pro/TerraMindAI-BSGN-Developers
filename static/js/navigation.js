(function () { function setSidebar(open) { const shell = document.querySelector('[data-sidebar-shell]'); const trigger = document.querySelector('[data-sidebar-toggle]'); if (!shell) return; shell.classList.toggle('sidebar-open', open); trigger?.setAttribute('aria-expanded', String(open)); document.body.style.overflow = open && window.innerWidth <= 820 ? 'hidden' : ''; } document.addEventListener('DOMContentLoaded', function () { document.querySelectorAll('[data-sidebar-toggle]').forEach(function (button) { button.addEventListener('click', function () { setSidebar(!document.querySelector('[data-sidebar-shell]')?.classList.contains('sidebar-open')); }); }); document.addEventListener('keydown', function (event) { if (event.key === 'Escape') setSidebar(false); }); window.addEventListener('resize', function () { if (window.innerWidth > 820) setSidebar(false); }); const path = window.location.pathname; document.querySelectorAll('.nav-item, .topnav a').forEach(function (link) { if (link.getAttribute('href') === path) link.setAttribute('aria-current', 'page'); }); }); }());
