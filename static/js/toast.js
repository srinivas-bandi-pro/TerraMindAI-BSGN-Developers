window.AgriSmartToast = (function () { function show(message, type, title) { const region = document.querySelector('[data-toast-region]'); const template = document.getElementById('toast-template'); if (!region || !template) return; const toast = template.content.firstElementChild.cloneNode(true); toast.classList.remove('toast--info'); toast.classList.add('toast--' + (type || 'info')); toast.querySelector('.toast__title').textContent = title || 'Notice'; toast.querySelector('.toast__message').textContent = message; const close = function () { toast.remove(); }; toast.querySelector('.toast__close').addEventListener('click', close); region.appendChild(toast); window.setTimeout(close, 5000); } return { show: show }; }());
