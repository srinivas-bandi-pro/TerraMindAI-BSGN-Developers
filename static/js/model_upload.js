document.addEventListener('DOMContentLoaded',()=>{const form=document.querySelector('[data-model-upload]');if(!form)return;const status=document.querySelector('[data-upload-status]');form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button[type="submit"]');button.disabled=true;status.textContent='Uploading model files…';try{const response=await fetch('/api/model/upload',{method:'POST',body:new FormData(form)});const body=await response.json();if(!response.ok||!body.success)throw new Error(body.error?.message||'Upload failed.');status.textContent=body.message;window.AgriSmartToast?.show(body.message,'success','Model upload')}catch(error){status.textContent=error.message;window.AgriSmartToast?.show(error.message,'error','Upload failed')}finally{button.disabled=false}})});
